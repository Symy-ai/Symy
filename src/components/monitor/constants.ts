/**
 * MonitorTab — 常量定义（从 monitor-tab.tsx 抽出，C3 拆分）
 *
 * 纯常量，无运行时逻辑。行为零变化。
 */

export const AUTO_SYNC_INTERVAL_MS = 5 * 60 * 1000; // 5 minutes
export const AUTO_SYNC_KEY = 'symy_last_auto_sync';
export const AUTO_SYNC_ENABLED_KEY = 'symy_auto_sync_enabled';
